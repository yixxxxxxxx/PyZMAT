# Workspace environment

For Python development and testing in this repository, initialize the project
environment in each new shell:

```bash
source ~/load_conda.sh
conda activate MACE_lora
```

Shell state does not persist between command invocations, so include both
commands before Python, test, or dependency commands.
